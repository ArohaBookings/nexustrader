pandas>=2.2.0
numpy>=1.26.0
PyYAML>=6.0.1
scikit-learn>=1.5.0
joblib>=1.4.2
pyarrow>=15.0.0
fastapi>=0.111.0
uvicorn>=0.30.0
ccxt>=4.4.0
pytest>=8.0.0
# Optional: install MetaTrader5 manually only on supported platforms/interpreters.
# The bot runs in safe simulated mode when the package is unavailable.
